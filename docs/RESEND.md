# Resend

Verifikasi domain, lalu isi `RESEND_API_KEY` dan `RESEND_FROM_EMAIL`. Email dikirim server-side setelah transaksi bisnis. `emailEvents` menyimpan idempotency key, penerima termasking, status, dan error aman. Email bukan sumber kebenaran status bisnis.
