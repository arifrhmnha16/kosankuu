import{FieldValue}from"firebase-admin/firestore";import{getSession}from"@/lib/auth/session";import{adminDb}from"@/lib/firebase/admin";import{complaintInput}from"@/lib/validation";import{success,resultError,AppError}from"@/lib/errors";import{cleanText}from"@/lib/security";import{sendTransactionalEmail}from"@/lib/email";export async function POST(req:Request){try{const s=await getSession();if(!s||s.role!=="tenant")throw new AppError("FORBIDDEN","Akses tenant diperlukan.",403);const input=complaintInput.parse(await req.json()),db=adminDb(),ref=db.collection("complaints").doc(),history=ref.collection("histories").doc(),now=FieldValue.serverTimestamp();await db.runTransaction(async tx=>{tx.set(ref,{...input,title:cleanText(input.title),description:cleanText(input.description),tenantId:s.uid,tenantEmail:s.email||"",status:"open",createdAt:now,updatedAt:now});tx.set(history,{actorId:s.uid,previousStatus:null,newStatus:"open",message:"Keluhan dibuat.",createdAt:now});tx.set(db.collection("activities").doc(),{actorId:s.uid,subjectType:"complaint",subjectId:ref.id,action:"created",description:"Keluhan baru dibuat.",createdAt:now})});if(s.email)void sendTransactionalEmail({to:s.email,subject:"Keluhan diterima",title:"Keluhan diterima",message:"Keluhan Anda telah tercatat dan akan ditindaklanjuti.",template:"complaint_received",relatedId:ref.id}).catch(()=>undefined);return Response.json(success({id:ref.id}),{status:201})}catch(error){const{body,status}=resultError(error);return Response.json(body,{status})}}
