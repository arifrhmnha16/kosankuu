import{getSession}from"@/lib/auth/session";import{restoreRoom}from"@/lib/workflows";import{success,resultError,AppError}from"@/lib/errors";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const s=await getSession();if(!s)throw new AppError("UNAUTHENTICATED","Silakan login.",401);const{id}=await params;await restoreRoom(s,id);return Response.json(success({id,restored:true}))}catch(error){const{body,status}=resultError(error);return Response.json(body,{status})}}
