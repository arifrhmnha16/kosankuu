import{z}from"zod";import{getSession}from"@/lib/auth/session";import{cancelTenantBooking}from"@/lib/workflows";import{success,resultError,AppError}from"@/lib/errors";import{assertSameOrigin}from"@/lib/security";export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){try{await assertSameOrigin(req);const s=await getSession();if(!s)throw new AppError("UNAUTHENTICATED","Silakan login.",401);const{id}=await params,{reason}=z.object({reason:z.string().min(5).max(500)}).parse(await req.json());await cancelTenantBooking(s,id,reason);return Response.json(success({id,status:"cancelled"}))}catch(error){const{body,status}=resultError(error);return Response.json(body,{status})}}
