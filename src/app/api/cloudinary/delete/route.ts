import{v2 as cloudinary}from"cloudinary";import{z}from"zod";import{getSession}from"@/lib/auth/session";import{success,resultError,AppError}from"@/lib/errors";export async function POST(req:Request){try{const s=await getSession();if(!s)throw new AppError("UNAUTHENTICATED","Silakan login.",401);const{publicId,purpose}=z.object({publicId:z.string().startsWith("manzsa-residence/"),purpose:z.enum(["room","gallery","avatar","complaint","payment","property"])}).parse(await req.json());if(["room","gallery","property","payment"].includes(purpose)&&s.role!=="owner")throw new AppError("FORBIDDEN","Akses hapus ditolak.",403);if(purpose==="avatar"&&!publicId.includes(`/users/${s.uid}/`))throw new AppError("FORBIDDEN","Aset bukan milik Anda.",403);cloudinary.config({cloud_name:process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME,api_key:process.env.CLOUDINARY_API_KEY,api_secret:process.env.CLOUDINARY_API_SECRET});await cloudinary.uploader.destroy(publicId,{type:purpose==="payment"?"authenticated":"upload",invalidate:true});return Response.json(success({publicId,deleted:true}))}catch(error){const{body,status}=resultError(error);return Response.json(body,{status})}}
