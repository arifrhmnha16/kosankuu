import{NextResponse}from"next/server";import{cookies}from"next/headers";import{adminAuth}from"@/lib/firebase/admin";import{env}from"@/lib/env";export async function POST(){const token=(await cookies()).get(env.SESSION_COOKIE_NAME)?.value;if(token){try{const s=await adminAuth().verifySessionCookie(token);await adminAuth().revokeRefreshTokens(s.uid)}catch{}}const res=NextResponse.json({ok:true});res.cookies.set(env.SESSION_COOKIE_NAME,"",{httpOnly:true,path:"/",maxAge:0});return res}
