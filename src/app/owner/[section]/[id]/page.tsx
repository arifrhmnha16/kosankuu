import{notFound}from"next/navigation";import{ownerSections}from"@/lib/navigation";import{requireRole}from"@/lib/auth/session";import{sectionDetail,listSection}from"@/lib/firestore/private-data";import{OwnerDetail}from"@/components/owner/owner-detail";import{AnnouncementForm}from"@/components/owner/announcement-form";export default async function Page({params}:{params:Promise<{section:string,id:string}>}){const{section,id}=await params,info=ownerSections[section as keyof typeof ownerSections];if(!info||!["kamar","booking","tenant","pembayaran","invoice","keluhan","pengumuman"].includes(section))notFound();const session=await requireRole("owner");if(section==="pengumuman"&&id==="new")return <><header className="dash-head"><h1>Buat Pengumuman</h1><p>Targetkan informasi kepada penghuni yang tepat.</p></header><AnnouncementForm/></>;const record=await sectionDetail(session,section,id);if(section==="pengumuman")return <><header className="dash-head"><h1>Edit Pengumuman</h1><p>{String(record.title)}</p></header><AnnouncementForm record={record}/></>;const relatedSection=section==="tenant"?["booking","pembayaran","invoice","keluhan"]:[],related=(await Promise.all(relatedSection.map(s=>listSection(session,s)))).flat().filter(r=>r.tenantId===record.id);return <><header className="dash-head"><h1>Detail {info[0]}</h1><p>Referensi {id}</p></header><OwnerDetail section={section} record={record} related={related}/></>}
