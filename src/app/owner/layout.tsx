import{DashboardShell}from"@/components/dashboard-shell";import{requireRole}from"@/lib/auth/session";export const metadata={title:{default:"Owner",template:"%s | Owner Manzsa"},robots:{index:false,follow:false}};export default async function Layout({children}:{children:React.ReactNode}){const s=await requireRole("owner");return <DashboardShell role="owner" name={s.name||s.email||"Owner"}>{children}</DashboardShell>}
