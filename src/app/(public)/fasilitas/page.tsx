import{CheckCircle2}from"lucide-react";import{publicFacilities}from"@/lib/firestore/public-data";export const metadata={title:"Fasilitas",alternates:{canonical:"/fasilitas"}};export default async function Page(){const items=await publicFacilities();return <section className="section"><div className="container"><p className="eyebrow">Fasilitas</p><h1 className="section-title">Kebutuhan sehari-hari, tersedia.</h1>{items.length?<div className="grid three">{items.map(item=><div className="card" key={String(item.id)}><CheckCircle2 size={30}/><h2 style={{fontSize:29,marginTop:20}}>{String(item.name)}</h2><p className="muted">{String(item.description||"Dikelola sebagai bagian dari kenyamanan bersama.")}</p></div>)}</div>:<div className="empty">Belum ada fasilitas publik.</div>}</div></section>}
