import{publicProperty}from"@/lib/firestore/public-data";import{ContactForm}from"@/components/contact-form";export const metadata={title:"Kontak",alternates:{canonical:"/kontak"}};export default async function Page(){const p=await publicProperty();return <section className="section"><div className="container contact-grid"><div><p className="eyebrow">Kontak</p><h1 className="section-title">Kami siap membantu.</h1><p><strong>WhatsApp</strong><br/>{String(p.whatsapp||"Belum diatur")}</p><p><strong>Email</strong><br/>{String(p.email)}</p><p><strong>Jam layanan</strong><br/>{String(p.operatingHours)}</p><p><strong>Alamat</strong><br/>{String(p.address)}</p></div><ContactForm/></div></section>}
