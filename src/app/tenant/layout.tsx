import{DashboardShell}from"@/components/dashboard-shell";import{requireRole}from"@/lib/auth/session";export const metadata={title:{default:"Tenant",template:"%s | Tenant Manzsa"},robots:{index:false,follow:false}};export default async function Layout({children}:{children:React.ReactNode}){const s=await requireRole("tenant");return <DashboardShell role="tenant" name={s.name||s.email||"Tenant"}>{children}</DashboardShell>}
