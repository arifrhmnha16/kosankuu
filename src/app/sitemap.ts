import type{MetadataRoute}from"next";import{env}from"@/lib/env";import{publicRooms}from"@/lib/firestore/public-data";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const paths=["","/kamar","/tentang","/fasilitas","/galeri","/faq","/kontak"],rooms=await publicRooms();return[...paths.map(url=>({url:`${env.NEXT_PUBLIC_APP_URL}${url}`,lastModified:new Date(),changeFrequency:"weekly"as const,priority:url===""?1:.7})),...rooms.map(r=>({url:`${env.NEXT_PUBLIC_APP_URL}/kamar/${r.slug}`,lastModified:new Date(),changeFrequency:"weekly"as const,priority:.8}))]}
