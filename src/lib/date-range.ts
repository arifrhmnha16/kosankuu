export type RangePreset="today"|"week"|"month"|"year"|"custom";
export function normalizeDateRange(preset:RangePreset,now=new Date(),custom?:{from:Date;to:Date}){const atStart=(d:Date)=>new Date(d.getFullYear(),d.getMonth(),d.getDate());let start=atStart(now),end=new Date(start);if(preset==="today")end.setDate(end.getDate()+1);else if(preset==="week"){start.setDate(start.getDate()-((start.getDay()+6)%7));end=new Date(start);end.setDate(end.getDate()+7)}else if(preset==="month"){start=new Date(now.getFullYear(),now.getMonth(),1);end=new Date(now.getFullYear(),now.getMonth()+1,1)}else if(preset==="year"){start=new Date(now.getFullYear(),0,1);end=new Date(now.getFullYear()+1,0,1)}else{if(!custom||custom.to<custom.from)throw new Error("Rentang tanggal tidak valid.");start=atStart(custom.from);end=atStart(custom.to);end.setDate(end.getDate()+1)}return{start,end}}
