import"server-only";import{FieldValue,Timestamp}from"firebase-admin/firestore";import{adminAuth,adminDb}from"@/lib/firebase/admin";import{AppError}from"@/lib/errors";import{lockIds}from"@/lib/booking";import{sendTransactionalEmail}from"@/lib/email";import type{DecodedIdToken}from"firebase-admin/auth";
const bookingTransitions:Record<string,string[]>={draft:["pending_approval","cancelled"],pending_approval:["pending_payment","rejected","cancelled"],pending_payment:["confirmed","cancelled","expired"],confirmed:["active","cancelled"],active:["completed","cancelled"],completed:[],cancelled:[],rejected:[],expired:[]};
const complaintTransitions:Record<string,string[]>={open:["in_progress","rejected"],in_progress:["waiting_tenant","resolved","rejected"],waiting_tenant:["in_progress","resolved"],resolved:["closed","in_progress"],closed:[],rejected:[]};
export function canTransition(kind:"booking"|"complaint",from:string,to:string){return(kind==="booking"?bookingTransitions:complaintTransitions)[from]?.includes(to)??false}
export async function transitionBooking(session:DecodedIdToken,id:string,to:string,reason?:string){if(session.role!=="owner")throw new AppError("FORBIDDEN","Akses owner diperlukan.",403);const db=adminDb(),ref=db.doc(`bookings/${id}`),notify={email:"",title:"",message:"",template:""};await db.runTransaction(async tx=>{const doc=await tx.get(ref);if(!doc.exists)throw new AppError("NOT_FOUND","Booking tidak ditemukan.",404);const data=doc.data()!;if(!canTransition("booking",data.status,to))throw new AppError("INVALID_TRANSITION",`Status ${data.status} tidak dapat diubah menjadi ${to}.`,409);if(["rejected","cancelled"].includes(to)&&!reason?.trim())throw new AppError("REASON_REQUIRED","Alasan wajib diisi.",422);const locks=await db.collection("bookingLocks").where("bookingId","==",id).get();const now=FieldValue.serverTimestamp();tx.update(ref,{status:to,statusReason:reason||null,updatedAt:now,...(to==="active"?{activatedAt:now}:{}),...(to==="completed"?{completedAt:now}:{})});if(["rejected","cancelled","completed"].includes(to))locks.docs.forEach(x=>tx.delete(x.ref));if(data.invoiceId&&["rejected","cancelled"].includes(to))tx.update(db.doc(`invoices/${data.invoiceId}`),{status:"cancelled",updatedAt:now});tx.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"booking",subjectId:id,action:`status_${to}`,description:`Status booking diubah menjadi ${to}${reason?`: ${reason}`:""}.`,createdAt:now});tx.set(db.collection("notifications").doc(),{userId:data.tenantId,type:"booking_updated",title:"Status booking diperbarui",message:`Booking ${data.code||id} kini ${to}.`,link:`/tenant/booking/${id}`,readAt:null,createdAt:now});Object.assign(notify,{email:data.snapshot?.tenant?.email||"",title:"Status booking diperbarui",message:`Booking ${data.code||id} kini berstatus ${to}.`,template:`booking_${to}`})});if(notify.email)void sendTransactionalEmail({...notify,to:notify.email,subject:notify.title,relatedId:id}).catch(()=>undefined)}
export async function cancelTenantBooking(session:DecodedIdToken,id:string,reason:string){const db=adminDb(),ref=db.doc(`bookings/${id}`);await db.runTransaction(async tx=>{const doc=await tx.get(ref);if(!doc.exists||doc.data()!.tenantId!==session.uid)throw new AppError("NOT_FOUND","Booking tidak ditemukan.",404);const data=doc.data()!;if(!["draft","pending_approval","pending_payment","confirmed"].includes(data.status))throw new AppError("INVALID_TRANSITION","Booking ini tidak dapat dibatalkan.",409);const locks=await db.collection("bookingLocks").where("bookingId","==",id).get(),now=FieldValue.serverTimestamp();tx.update(ref,{status:"cancelled",statusReason:reason,updatedAt:now});locks.docs.forEach(x=>tx.delete(x.ref));if(data.invoiceId)tx.update(db.doc(`invoices/${data.invoiceId}`),{status:"cancelled",updatedAt:now});tx.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"booking",subjectId:id,action:"cancelled",description:`Booking dibatalkan: ${reason}`,createdAt:now})})}
export async function transitionComplaint(session:DecodedIdToken,id:string,to:string,message:string){if(session.role!=="owner")throw new AppError("FORBIDDEN","Akses owner diperlukan.",403);const db=adminDb(),ref=db.doc(`complaints/${id}`);let email="";await db.runTransaction(async tx=>{const doc=await tx.get(ref);if(!doc.exists)throw new AppError("NOT_FOUND","Keluhan tidak ditemukan.",404);const from=doc.data()!.status;if(!canTransition("complaint",from,to))throw new AppError("INVALID_TRANSITION",`Status ${from} tidak dapat diubah menjadi ${to}.`,409);const now=FieldValue.serverTimestamp();tx.update(ref,{status:to,ownerResponse:message,updatedAt:now,...(to==="resolved"?{resolvedAt:now}:{}),...(to==="closed"?{closedAt:now}:{})});tx.set(ref.collection("histories").doc(),{actorId:session.uid,previousStatus:from,newStatus:to,message,createdAt:now});tx.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"complaint",subjectId:id,action:`status_${to}`,description:message,createdAt:now});tx.set(db.collection("notifications").doc(),{userId:doc.data()!.tenantId,type:"complaint_updated",title:"Keluhan diperbarui",message,link:`/tenant/keluhan/${id}`,readAt:null,createdAt:now});email=doc.data()!.tenantEmail||""});if(email)void sendTransactionalEmail({to:email,subject:"Keluhan diperbarui",title:"Keluhan diperbarui",message,template:"complaint_updated",relatedId:`${id}_${to}`}).catch(()=>undefined)}
export async function setTenantActive(session:DecodedIdToken,uid:string,active:boolean,note?:string){if(session.role!=="owner")throw new AppError("FORBIDDEN","Akses owner diperlukan.",403);const user=await adminAuth().getUser(uid);await adminAuth().updateUser(uid,{disabled:!active});if(!active)await adminAuth().revokeRefreshTokens(uid);const db=adminDb(),batch=db.batch(),now=FieldValue.serverTimestamp();batch.set(db.doc(`users/${uid}`),{status:active?"active":"inactive",disabled:!active,internalNote:note||null,updatedAt:now},{merge:true});batch.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"user",subjectId:uid,action:active?"activated":"deactivated",description:`Akun ${user.email||uid} ${active?"diaktifkan":"dinonaktifkan"}.`,createdAt:now});await batch.commit()}
export async function deleteTenantAccount(session:DecodedIdToken,uid:string){if(session.role!=="owner")throw new AppError("FORBIDDEN","Akses owner diperlukan.",403);const db=adminDb(),profile=await db.doc(`users/${uid}`).get();if(!profile.exists||profile.data()?.role!=="tenant")throw new AppError("NOT_FOUND","Tenant tidak ditemukan.",404);const bookings=await db.collection("bookings").where("tenantId","==",uid).limit(100).get();if(bookings.docs.some(doc=>["pending_payment","confirmed","active"].includes(doc.data().status)))throw new AppError("TENANT_HAS_ACTIVE_BOOKING","Akun dengan booking atau sewa aktif tidak dapat dihapus.",409);await adminAuth().deleteUser(uid);const batch=db.batch(),now=FieldValue.serverTimestamp();batch.delete(db.doc(`users/${uid}`));batch.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"user",subjectId:uid,action:"account_deleted",description:"Akun tenant dihapus. Riwayat transaksi historis tetap dipertahankan.",createdAt:now});await batch.commit()}
export async function createAnnouncementNotifications(announcementId:string,data:Record<string,unknown>){if(data.status!=="published")return;const db=adminDb();let query:FirebaseFirestore.Query=db.collection("users").where("role","==","tenant").where("status","==","active");if(data.audienceType==="tenant")query=query.where("uid","==",data.targetUserId);if(data.audienceType==="room")query=query.where("activeRoomId","==",data.targetRoomId);const users=await query.limit(500).get(),batch=db.batch(),now=FieldValue.serverTimestamp();for(const user of users.docs)batch.set(db.collection("notifications").doc(),{userId:user.id,type:"announcement",title:data.title,message:String(data.content).slice(0,180),link:`/tenant/pengumuman/${announcementId}`,readAt:null,createdAt:now});await batch.commit();if(data.priority==="important")for(const user of users.docs){const email=user.data().email;if(email)void sendTransactionalEmail({to:email,subject:String(data.title),title:String(data.title),message:String(data.content),template:"important_announcement",relatedId:`${announcementId}_${user.id}`}).catch(()=>undefined)}}
export async function restoreRoom(session:DecodedIdToken,id:string){if(session.role!=="owner")throw new AppError("FORBIDDEN","Akses owner diperlukan.",403);const db=adminDb(),now=FieldValue.serverTimestamp(),batch=db.batch();batch.update(db.doc(`rooms/${id}`),{archivedAt:null,status:"inactive",isPublic:false,updatedAt:now});batch.set(db.collection("activities").doc(),{actorId:session.uid,subjectType:"room",subjectId:id,action:"restored",description:"Kamar dipulihkan sebagai inactive dan private.",createdAt:now});await batch.commit()}
export function bookingLockIds(data:{roomId:string;rentalType:"hourly"|"daily"|"monthly";startAt:{toDate():Date};endAt:{toDate():Date}}){return lockIds(data.roomId,data.rentalType,data.startAt.toDate(),data.endAt.toDate())}
