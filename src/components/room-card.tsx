import Link from"next/link";import Image from"next/image";import{BedDouble}from"lucide-react";import type{Room}from"@/types/domain";import{rupiah}from"@/lib/format";export function RoomCard({room}:{room:Room}){const price=room.pricing.monthly||room.pricing.daily||room.pricing.hourly,image=room.images?.sort((a,b)=>a.sortOrder-b.sortOrder)[0];return <article className="card room-card">{image?<div className="room-image"><Image src={image.secureUrl} alt={image.altText} fill sizes="(max-width: 600px) 100vw, 33vw" style={{objectFit:"cover"}}/></div>:<div className="room-image"><BedDouble size={48} strokeWidth={1}/></div>}<div className="room-body"><div className="room-meta"><span>{room.type} · {room.area} m²</span><span className="status">{room.status==="available"?"Tersedia":"Jadwal terbatas"}</span></div><h3>{room.name}</h3><p className="muted">{room.description}</p><p className="price">Mulai {rupiah(price)}</p><Link className="button outline" href={`/kamar/${room.slug}`}>Lihat kamar</Link></div></article>}
