import{Inbox}from"lucide-react";export function EmptyState({title="Belum ada data",message="Data akan tampil setelah tersedia."}:{title?:string;message?:string}){return <div className="empty"><Inbox/><h2>{title}</h2><p className="muted">{message}</p></div>}export function LoadingCards(){return <div className="grid three">{[1,2,3].map(x=><div className="card" key={x}><div className="skeleton"/><div style={{height:14}}/><div className="skeleton"/></div>)}</div>}
